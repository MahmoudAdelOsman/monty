#include "monty.h"

/**
 * func_to_queue - convertes a stack to a queue
 * @nu: pointer to the top element of a stack
 * @l: line number
 * Return: nothing
 */

void func_to_queue(stc_t **nu, unsigned int l)
{
	momo->stk = 1;
	(void) nu;
	(void) l;
}
