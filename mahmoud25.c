#include "monty.h"

/**
 * func_to_stack - convertes a queue to a stack
 * @nu: pointer to the top element of a stack
 * @l: line number
 * Return: nothing
 */

void func_to_stack(stc_t **nu, unsigned int l)
{
	momo->stk = 0;
	(void) nu;
	(void) l;
}
